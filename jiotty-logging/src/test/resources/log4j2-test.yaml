Configuration:
  #  status: debug
  appenders:
    Console:
      name: STDOUT
      PatternLayout:
        Pattern: "%d %p [%t] %C{1.} %m%n"
  Loggers:
    Root:
      level: info
      AppenderRef:
        - ref: STDOUT